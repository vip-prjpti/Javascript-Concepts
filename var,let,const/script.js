let a = 10;
console.log(a);

var b = 100;

//let and const are hoisted and they are in the Temporal dead zone since they are hoisted and the moment they are initialized a value.

//they are in a separate space - script scope
//let and const are not accessible till they are assigned with some values.